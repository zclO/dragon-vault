[package]
name = "dragon-vault"
version = "0.1.0"
description = "A secure vault for managing LLM API keys"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "dragon_vault_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
aes-gcm = "0.10"          # AES-256-GCM 对称加密
argon2 = "0.5"            # Argon2id 密钥派生
base64 = "0.22"           # 密文信封编码
chrono = "0.4"            # 时间戳处理
rand = "0.8"              # 安全随机数（Salt/Nonce 生成）
regex = "1"               # API Key 格式校验
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }  # Key 连通性测试（rustls 避免安卓侧 OpenSSL 依赖）
uuid = { version = "1", features = ["v4"] }  # 唯一标识符生成
zeroize = { version = "1", features = ["alloc"] }   # 敏感数据内存清零


# Read the optimization guideline for more details: https://tauri.app/concept/size/#cargo-configuration
[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
